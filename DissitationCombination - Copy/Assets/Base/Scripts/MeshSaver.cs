using UnityEngine;
using System.Collections;
//using UnityEditor;

//David Cope 
//A Tool for Wing Creation
//53037834
//This line is 90 chars long ------------------------------------------------------------

    //An expserimental mesh saver class, please see final report for more details

public class MeshSaver : MonoBehaviour {

    public Mesh inMesh;
    bool doOnce = true;
	// Update is called once per frame
	void Update () {

        //if(Input.GetKeyDown(KeyCode.B))

        //    if (doOnce)
        //    {
        //        inMesh = GameObject.Find("wingMesh").GetComponent<MeshFilter>().mesh;
        //        AssetDatabase.CreateAsset(inMesh, "Assets/MESH.obj");
        //        AssetDatabase.SaveAssets();

        //        doOnce = false;
        //    }
     
	}
}
